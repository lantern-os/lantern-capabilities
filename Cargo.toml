[package]
name = "lantern-capabilities"
version = "0.0.0"
edition = "2021"
description = "LanternOS service-layer capability broker — Phase 2 prototype (RFC-0009/RFC-0010)"
license = "MIT OR Apache-2.0"
publish = false

[lib]
name = "lantern_capabilities"
path = "src/lib.rs"

[dependencies]
lantern-hal = { git = "https://github.com/lantern-os/lantern-hal", branch = "main" }
lantern-kernel = { git = "https://github.com/lantern-os/lantern-kernel", branch = "main" }
